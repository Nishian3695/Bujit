<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/expense_table_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clipChildren="true"
    android:longClickable="true"
    android:orientation="vertical">

    <!-- Accent bar — visible only for Teller-linked expenses -->
    <View
        android:id="@+id/linked_indicator"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@color/primary_blue"
        android:visibility="gone" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipChildren="true"
        android:paddingTop="14dp"
        android:paddingBottom="14dp">

        <!-- Content fills the full row width. -->
        <LinearLayout
            android:id="@+id/expense_content"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/expense_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="3"
                android:ellipsize="end"
                android:fontFamily="sans-serif"
                android:singleLine="true"
                android:textColor="?attr/colorOnSurface"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/expense_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="2.5"
                android:alpha="0.55"
                android:fontFamily="sans-serif"
                android:gravity="center"
                android:maxLines="1"
                android:textColor="?attr/colorOnSurface"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/expense_rate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="2"
                android:alpha="0.45"
                android:fontFamily="sans-serif"
                android:gravity="center"
                android:textColor="?attr/colorOnSurface"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/expense_cost"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="2"
                android:fontFamily="sans-serif-medium"
                android:gravity="end"
                android:maxLines="1"
                android:textColor="?attr/colorPrimary"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/expense_status"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:visibility="gone" />

        </LinearLayout>

        <!--
            Checkbox slides in over the content start. layout_height="0dp" with
            top/bottom constraints forces ConstraintLayout to clamp its height to
            the content row — the widget's own Material touch-target minimum is ignored.
        -->
        <CheckBox
            android:id="@+id/expense_checkbox"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:alpha="0"
            android:clickable="false"
            android:focusable="false"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/expense_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/expense_content" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Row divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="?attr/colorControlHighlight" />

</LinearLayout>
